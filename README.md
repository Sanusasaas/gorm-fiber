# Fiber Project with Postgres

**REST API на Go с использованием Fiber, Postgres и Gorm.**

---

## 📌 О проекте
Этот проект — бэкенд для проведения CRUD операций над базой данных.
- **Fiber** — фреймворк для создания HTTP-сервера.
- **Postgres** — база данных для хранения информации.
- **Docker** — для развёртывания и управления зависимостями.

---

## 🛠 Установка и запуск

1. Установи Docker и Docker Compose. (https://docs.docker.com/get-docker/) (https://docs.docker.com/compose/install/)
2. Клонируй репозиторий и перейди в папку: \
    git clone https://github.com/Sanusasaas/gorm-fiber-postgres \
    cd fiber-postgres-gorm
3. Запусти проект:
    docker-compose up --build
Приложение будет доступно на http://localhost:8080

## 🚀  API Эндпоинты
[POST]    /api/create                   Создание машшины. \
[GET]     /api/cars                     Получение списка машин. \
[GET]     /api/car/:id                  Получение информации о машине по id \
[PATCH]   /api/change/:id               Обновление стоимости машины.\
[DELETE]  /api/deleteall                Удаление всех элементов из базы данных \
[DELETE]  /api/delete/:id               Удаление элемента из базы данных по id

## Пример запроса на создание элемента:
curl -X POST http://localhost:8080/api/create   \
  -H "Content-Type: application/json" \
  -d '{
    "brand": "Toyota", \
    "name": "Supra", \
    "price": 20000.50 \
  }'

## Пример запроса на получение всех элементов из базы данных:
curl -X GET http://localhost:8080/api/cars

## Зависимости

Fiber — веб-фреймворк. (https://github.com/gofiber/fiber/v2) \
PostgresDB — для работы с Postgres. (https://gorm.io/driver/postgres) \
Godotenv — для загрузки переменных окружения из .env. (https://github.com/joho/godotenv)
